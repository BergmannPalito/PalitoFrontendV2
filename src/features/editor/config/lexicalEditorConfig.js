// src/features/patents/config/lexicalConfig.js

// Basic Theme for the Lexical Editor used in Description display
export const editorTheme = {
    text: {
        // Class name applied to highlighted text nodes
        highlight: 'highlight', // Ensure .highlight class is defined in global.css
    },
    // Add other theme configurations if needed (e.g., paragraph styles)
    // paragraph: 'your-paragraph-class',
};

// You could also export shared node lists or other configurations from here later.
// export const editorNodes = [...];